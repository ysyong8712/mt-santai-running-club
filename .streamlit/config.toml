[theme]
primaryColor = "#FF4D00"
backgroundColor = "#0C0C0C"
secondaryBackgroundColor = "#161616"
textColor = "#F0EBE1"
font = "sans serif"
```

保存。文件结构应该是这样：
```
mt-santai-running-club\
    app.py
    requirements.txt
    runtime.txt
    .streamlit\
        config.toml